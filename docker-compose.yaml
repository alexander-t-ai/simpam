version: "3.9"

services:
  ipam:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ipam_data:/data
      - ./initializers:/initializers
    restart: unless-stopped

volumes:
  ipam_data:
